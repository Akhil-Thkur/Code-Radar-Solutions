#include<stdio.h>
int main () {
    char str[100];
    scanf("%s",&str);
    if (char str>A && char str<Z) {
        printf("uppercase");
    }
    else {
        printf("Lowercase");
    }
    return 0;
}