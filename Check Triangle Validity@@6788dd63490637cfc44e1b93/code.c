#include<stdio.h>
int main () {
    int x,y,z;
    scanf("%d",&x);
    scanf("%d",&y);
    scanf("%d",&z);
    if (a+b>c && b+c>a && a+c>b) {
        printf("Valid");
    }
    else {
        printf("Invalid")
    }
}